{% extends 'base.html.twig' %}

{% block body %}
    <h1>Produto 
        <a href="{{ path('produtos_index') }}" class="btn btn-primary" title="Voltar à lista">
            <span class="glyphicon glyphicon-menu-hamburger"></span>
        </a>
        <a href="{{ path('produtos_edit', { 'id': produto.id }) }}" title="Editar" class="btn btn-default">
            <span class="glyphicon glyphicon-pencil"></span>
        </a>
    </h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produto.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ produto.nome }}</td>
            </tr>
            <tr>
                <th>Quantidade</th>
                <td>{{ produto.quantidade }}</td>
            </tr>
            <tr>
                <th>Preco</th>
                <td>{{ produto.preco }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ produto.tipo }}</td>
            </tr>
            <tr>
                <th>Imagem</th>
                <td>{{ produto.imagem }}</td>
            </tr>
        </tbody>
    </table>

    
            
        
            
        
            {{ form_start(delete_form) }}
                <input type="submit" value="Excluir" class="btn btn-warning">
            {{ form_end(delete_form) }}
        
{% endblock %}
